server:
  port: 8888

spring:
  application:
    name: config-server-svc
  cloud:
    config:
      server:
        git:
          uri: https://github.com/imjoereymond/config-server-repo
          clone-on-start: true
          default-label: development
          search-paths: wellcare-hub
  security:
    user:
      name: technoo
      password: <9LLW342'AJR

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*'
  security:
    enabled: true
